// const pinsInfo = (pinsObject) => {
//   document.querySelector('#content-container').innerHTML += `<h1 class="text-white">${pinsObject.title} ${pinsObject.boards_id}'s Pins</h1>`;
// };

// export default pinsInfo;
